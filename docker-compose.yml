services:
  web:
    build: .
    command: ["python", "apps/web/run_web.py"]
    ports:
      - "5000:5000"
    environment:
      - PYTHONPATH=/app

  cli:
    build: .
    command: ["python", "apps/cli/main.py"]
    stdin_open: true
    tty: true
    environment:
      - PYTHONPATH=/app
