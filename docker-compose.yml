services:
  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rokoroot}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-roko}
      MYSQL_USER: ${MYSQL_USER:-roko}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-rokopass}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - roko_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${MYSQL_ROOT_PASSWORD:-rokoroot}"]
      interval: 5s
      timeout: 3s
      retries: 20

  adminer:
    image: adminer:4
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: db
      ADMINER_DESIGN: dracula
    ports:
      - "8080:8080"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: roko-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: mysql+pymysql://${MYSQL_USER:-roko}:${MYSQL_PASSWORD:-rokopass}@db:3306/${MYSQL_DATABASE:-roko}?charset=utf8mb4
      TZ: America/Sao_Paulo
    ports:
      - "8000:8000"
    volumes:
      - ${HOME}/.local/share/evove:/root/.local/share/evove:ro

  # Public HTTPS entrance for the API (api.voide.shop), so the APK works off the
  # home network. The tunnel is managed in the Cloudflare dashboard, where its
  # public hostname points at http://backend:8000; this container only holds the
  # connection open. TUNNEL_TOKEN lives in tunnel.env, which is not versioned.
  # Opt-in, since only the machine serving the API runs it:
  #   docker compose --profile tunnel up -d tunnel
  tunnel:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    profiles: [tunnel]
    depends_on:
      - backend
    command: tunnel --no-autoupdate run
    env_file: tunnel.env

  cli:
    build:
      context: .
      dockerfile: apps/cli/Dockerfile
    image: roko-cli
    stdin_open: true
    tty: true
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: mysql+pymysql://${MYSQL_USER:-roko}:${MYSQL_PASSWORD:-rokopass}@db:3306/${MYSQL_DATABASE:-roko}?charset=utf8mb4
      TZ: America/Sao_Paulo
    volumes:
      - ${HOME}/.local/share/evove:/root/.local/share/evove
      - ${HOME}/journal:/root/journal

volumes:
  roko_mysql_data:
